import React from "react";

const PageLoader = props => (
  <div className="PageLoader col">
    <div className="spinner-border" role="status">
      <span className="sr-only">Loading...</span>
    </div>
  </div>
);

export default PageLoader;
